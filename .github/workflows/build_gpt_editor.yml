name: Build GPT Editor

# 触发条件：推送到 emmc 分支或创建 PR 针对 emmc 分支
on:
  push:
    branches:
      - emmc
    # 可选：如果希望在推送标签时创建 Release，可以添加 tags 的触发
    tags:
      - 'v*.*.*'  # 例如 v1.0.0
  pull_request:
    branches:
      - emmc

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 步骤 1：检出仓库代码
      - name: Checkout repository
        uses: actions/checkout@v3

      # 步骤 2：安装 Python 2.7
      - name: Install Python 2.7
        run: |
          sudo apt-get update
          sudo apt-get install -y python2
          python2 --version

      # 步骤 3：安装 Python2 依赖（如 argparse）
      - name: Install Python2 dependencies
        run: |
          sudo apt-get install -y python-pip
          pip2 install argparse

      # 步骤 4：准备输入 JSON 文件
      - name: Prepare input JSON files
        working-directory: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor
        run: |
          mkdir -p gpt_output/mt798x
          echo '{"partition": "example", "size": "4MB"}' > gpt_output/mt798x/example.json
          echo "Created example input JSON files in gpt_output/mt798x/"
          ls -al gpt_output/mt798x

      # 步骤 5：赋予 generate_gpt.sh 执行权限
      - name: Make generate_gpt.sh executable
        working-directory: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor
        run: |
          chmod +x generate_gpt.sh

      # 步骤 6：运行 generate_gpt.sh 脚本，并打印详细日志
      - name: Generate GPT binaries
        working-directory: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor
        run: |
          set -x  # 打印所有执行命令
          ./generate_gpt.sh
          echo "Finished running generate_gpt.sh"
          echo "Contents of gpt_output:"
          ls -al gpt_output || echo "No gpt_output directory found"

      # 步骤 7：验证 gpt_output 目录是否生成
      - name: Verify gpt_output directory
        working-directory: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor
        run: |
          if [ ! -d "./gpt_output" ]; then
            echo "Build failed: gpt_output directory not found!"
            exit 1
          fi
          echo "Generated GPT binaries:"
          ls -al gpt_output

      # 步骤 8：上传 GPT 产物到 GitHub Actions Artifact
      - name: Upload GPT Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: gpt_binaries
          path: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor/gpt_output/**

      # 可选步骤：如果是标签推送，则创建 GitHub Release 并上传产物
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload Release Assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor/gpt_output/
          asset_name: gpt_binaries.zip
          asset_content_type: application/zip

      # 可选步骤：将 gpt_output 压缩为 zip 文件以便上传到 Release
      - name: Zip GPT Binaries
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          cd bl-mt798x/atf-20231013-0ea67d76a/tools/dev/gpt_editor
          zip -r gpt_binaries.zip gpt_output
